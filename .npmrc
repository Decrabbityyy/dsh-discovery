# The @deepseek-ai/dsh-* peers are injected by the harness host at runtime, so
# they are never auto-installed; each package pins the compile-time copy it
# builds against in devDependencies (see pnpm-workspace.yaml).
auto-install-peers=false
